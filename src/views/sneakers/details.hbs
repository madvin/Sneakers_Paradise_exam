
    <section class="details-hero">
        <div class="wrap">
            <h2>Sneakers Details</h2>
        </div>
    </section>

    <section class="details-content">
        <div class="wrap">
            <div class="sneakers-details">
                <div class="sneakers-image">
                    <img src="{{image}}" alt="{{model}}">
                </div>
                <div class="sneakers-info">
                    <h3>Brand: {{brand}}</h3>
                    <p><strong>Model:</strong> {{model}}</p>
                    <p><strong>Price:</strong> ${{price}}</p>
                    <p><strong>Size:</strong> {{size}}</p>
                    <p><strong>Condition:</strong> {{condition}}</p>
                    <p><strong>Model Year:</strong> {{year}}</p>
                    <p><strong>Description:</strong> {{description}}</p>
                </div>
                
            </div>
            <div class="buttons-wrap">
                {{#if isOwner}}
                <button class="btn edit"><a href="/sneakers/edit/{{id}}">Edit</a></button>
                <button class="btn delete"><a href="/sneakers/edit/{{id}}">Delete</a></button>
                {{/if}}
                {{#if isLoggedIn}}
                <button class="btn prefer"><a href="">Prefer</a></button>
                {{/if}}
                 <p class="prefer-message">You've already preferred this sneakers!</p>
            </div>
        </div>
    </section>
